import 'package:sqflite/sqflite.dart' as sql;
import 'package:path/path.dart' as path;

class DatabaseHelper {
  // How can you make sure you only ever have one instance of this class?
  // static final ...

  // You'll also need a variable to hold the database itself once it's opened.
  // static sql.Database? _database;

  // Next, you'll need a method to open (or create) the database.
  // Future<sql.Database> get database async { ... }
}