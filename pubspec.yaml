name: projects
description: "Testing the flutter framework"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  sqflite: ^2.4.2
  path: ^1.9.1
  image_picker: ^1.2.0
  video_thumbnail: ^0.5.6
  video_player: ^2.10.0
  fl_chart: ^1.1.1
  path_provider: ^2.1.5 # Added for testing video uploads
  easy_video_editor: ^0.1.3
  firebase_auth: ^6.1.1
  firebase_core: ^4.2.0
  cloud_firestore: ^6.0.3
  app_links: ^6.4.1 # Replaced uni_links with this line
  firebase_storage: ^13.0.3
  http: ^1.5.0

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/videos/sample.mp4
